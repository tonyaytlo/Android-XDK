<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.layer.ui.message.file.FileMessageModel"/>

        <variable
            name="viewModel"
            type="FileMessageModel"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:id="@+id/root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/file_icon"
            android:layout_width="@dimen/layer_ui_file_message_image_width"
            android:layout_height="@dimen/layer_ui_file_message_image_height"
            android:adjustViewBounds="true"
            android:scaleType="center"
            android:src="@drawable/ui_ic_file_generic"
            app:imageResource="@{viewModel.fileIconDrawable}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHeight_default="wrap"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_default="spread"/>
    </android.support.constraint.ConstraintLayout>

</layout>