<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View"/>

        <variable
            name="viewModel"
            type="com.layer.ui.message.MessageItemStatusViewModel"/>
    </data>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/layer_ui_message_item_top_bottom_margin"
        android:layout_marginEnd="@dimen/layer_ui_padding_normal"
        android:layout_marginLeft="@dimen/layer_ui_padding_normal"
        android:layout_marginRight="@dimen/layer_ui_padding_normal"
        android:layout_marginStart="@dimen/layer_ui_padding_normal"
        android:layout_marginTop="@dimen/layer_ui_message_item_top_bottom_margin"
        android:gravity="center"
        android:text="@{viewModel.text}"
        android:textColor="@color/layer_ui_text_light_gray"
        android:textSize="@dimen/layer_ui_text_size_status"
        android:visibility="@{viewModel.visible ? View.VISIBLE : View.GONE}"
        tools:text="Test Status"/>

</layout>
